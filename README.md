# MiniRag
this is a minimal implementation of the rag model for questoin answering 
## Rquirmentes
Python 3.13.11
### install python using miniconda 
1) download and install miniconda from
2) d
3)  cc
# installation 
### install the rquirmente packages
```bash
$ pip install -r requirmente.txt
```
### setup the environment virables
```bash
$ cb .env.example .env
```
setup the environment virables in the `.env` file, like `OPENAI_API_KEY` value.
